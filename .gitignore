# Fichiers système macOS
.DS_Store
**/.DS_Store

# Cursor IDE (paramètres locaux du workspace)
.cursor/

# Secrets locaux (HelloAsso, URL Sheet, etc.)
.env
.env.local

# Dependencies
node_modules/

# Build output (sources stay in repo; only dist/ is deployed)
dist/

# Certificats dev HTTPS (générés par scripts/gen-dev-cert.sh)
scripts/certs/dev.key
scripts/certs/dev.pem

# Local dev-only Malix access override (build injection)
malix/assets/access-config.local.js
